# 开发日志 - GitHub 开源项目浏览器

## v2.7.0（2026-09-07）
- **修复热榜列表翻译后排序错乱**：热榜列表项翻译完成回调误用"发现页模板"覆盖整行文本，把 `#排名`/🔺🔻🆕 涨跌前缀抹掉——已翻译的行显示成纯星星格式、未翻译的行仍是 `#1 #2…`，视觉上"前边按星星排、后边按 1-18 排名"。修复：抽取 `_trending_item_text`（统一构造带排名前缀的热榜文本），翻译回调按列表类型选模板，热榜行翻译后保留官方排名与涨跌标记。
- **发现页排序统一**：星星降序为主键，星星为 0 或相同按最近更新时间（`pushed_at`/`updated_at`）降序兜底（两次稳定排序实现）；GitHub API 不提供浏览量数据，故无浏览排序。
- **发现页滚动加载更多（无上限）**：原固定 50 条一次加载完。现每批渲染 10 条，滚动到底自动请求下一页（`sort=stars`，50/页），追加数据天然全局有序、不重排已渲染行避免滚动跳变；API 返回空或请求失败即停止并提示"已全部加载"。刷新列表时递增分页代数，丢弃在途的过期分页结果，防止旧数据串入新列表。
- **分类行为重构（热榜按领域重新抓取 + 发现页分类重新搜索）**：
  - 发现页选中有语言映射的分类（如 AI→python/jupyter notebook）→ 按 `language:A OR language:B` 重新搜索（`search_repos` 新增 `languages`/`sort` 参数，分类查询忽略顶部语言下拉，避免双语言条件冲突），列表只显示该分类项目并按星星排序；无语言映射的分类（安全）本地过滤兜底。
  - 趋势热榜选中有语言映射的分类 → 按分类语言分别抓取 `github.com/trending/{语言}` 页面，合并去重后按今日新增星星降序重排名次（官方排名本质按 period_stars）；Trending URL 语言需转连字符（`jupyter notebook → jupyter-notebook`）；无语言映射的分类本地过滤兜底，刷新热榜后分类仍生效。
- 版本号 2.6.0 → 2.7.0。

## v2.6.0（2026-09-07）
- **实现翻译供应商分发（方案A）**：此前设置里的供应商下拉框形同虚设——`translate_text` 与 `translate_readme_markdown` 忽略 `translation_provider`，一律走 OpenAI-compatible 接口，`fallback_chain` 从未被读取，翻译质量完全取决于用户配置的单个 LLM 模型。
  - 新增腾讯云 TMT 翻译引擎：复用 `translator.py` 现成的 TC3-HMAC-SHA256 签名（`tc3_authorization`），调用 `tmt.tencentcloudapi.com` 的 `TextTranslate`（Version 2018-03-21），使用配置中的 `tencent_secret_id/tencent_secret_key`，国内直连、有免费额度，单次上限 2000 字符，超长自动降级。注意：域名必须是 `tmt.tencentcloudapi.com`（不是 `mt.`），否则报 `action not found in service=mt`。
  - 新增 DeepL / Google 免费引擎：deep-translator 库此前零调用，现接入 `DeeplTranslator`（用 `translation_api_key` 作为 DeepL 密钥）与 `GoogleTranslator`（免费无需密钥，语言代码规范化为 `zh-CN` 格式）。
  - 新增 Edge 微软翻译引擎（免 key、国内直连）：浏览器页面翻译同款接口 `edge.microsoft.com/translate/translatetext`（api-version=3.0，POST JSON 数组，无需任何认证），语言代码映射 `zh-CN → zh-CHS`、`zh-TW → zh-CHT`。国内直连、翻译质量与腾讯云同级，是最省心的免费方案。
  - 新增本地词典兜底：`translator.py` 的 1900 行词典（`local_translate`）此前导入了却从未调用，现作为回退链末端兜底。
  - `auto` 模式主引擎优先级：已配置 LLM > 腾讯云 > Edge 微软 > Google。
  - 回退链生效：主供应商失败后沿 `fallback_chain` 依次尝试，最终本地词典兜底；README 因结构复杂仅走 LLM（保留 Markdown 格式）与本地词典（逐词替换不破坏结构）。
- **修复翻译请求挂死**：deep-translator 的 Google/DeepL 请求无 timeout 参数，访问被墙域名时无限挂起导致翻译卡死。新增 `_with_socket_timeout`（临时设置 socket 默认超时 6s）包装两个引擎调用。
- **修复列表翻译卡住（翻译到 N 个后不动）**：
  - 详情翻译与列表翻译原共用 `translate_pool`，详情页 README 长任务（最长 60s）会占满线程池，导致列表翻译任务排队无线程可用、进度停摆。新增独立单线程 `detail_translate_pool`，详情翻译（描述+README）改走该池，与列表翻译彻底隔离。
  - 列表翻译任务体包 try/except（异常返回 None 不中断进度）；`on_done/on_error` 补位改用 try/finally，保证无论结果如何都触发 QTimer 补位下一个任务，杜绝队列停摆。
  - 默认回退链移除国内不可用的 `deepl/google`（每次失败白白等 6s 超时），改为 `tencent → microsoft → openai_compatible`；用户回退链同步改为 `openai_compatible → microsoft → local`。
  - 微软翻译请求超时受总 deadline 约束（`timeout = min(8, 剩余时间)`），回退场景不再无谓拖长。
- 用户翻译供应商由 MiMo 切换为腾讯云，后又切为 Edge 微软翻译（主引擎）。
- 版本号 2.5.1 → 2.6.0，重新打包 exe。

## v2.5.1（2026-09-07）
- **修复详情页 401 Unauthorized（配置污染）**：`save_config_file` 中 `protect_sensitive_config` 原地将 `github_token` 等敏感字段加密为 `dpapi:v1:`，而 `HttpClient` 持有 `self.config` 同一字典引用——任意一次保存配置（收藏、切主题、设置）后，内存中 token 被污染成加密串，此后所有 GitHub API 请求返回 401。修复：`save_config_file` 在 `copy.deepcopy` 副本上做规范化与加密，不再修改调用方字典；磁盘仍写入加密值，内存保持明文。验证：保存后内存 token 不变、API /user 返回 200。
- 版本号 2.5.0 → 2.5.1，重新打包 exe。

## v2.5.0（2026-09-07）
- **合并"翻译项目名称"与"自动翻译项目简介"为一个选项**：删除冗余的"翻译项目名称"开关（原配置 `translate_name` 在功能代码中从未被读取，勾选无效果），合并为"自动翻译项目名称和简介"选项。列表翻译任务同时翻译项目名称（`full_name`）与简介，翻译后列表项显示中文名；激活 `auto_translate_desc` 配置——取消勾选后列表不再自动翻译。
- 版本号 2.4.0 → 2.5.0，重新打包 exe。

## v2.4.0（2026-09-07）
- **修复翻译缓存不生效（三个根因）**：
  1. `TranslationCache.get` 把目标语言（`source` 参数实际传入 `target`）误当作 `ttl_seconds` 传给 `get_translation`，SQLite 将 `'zh-CN'` 转为 0，TTL 条件变为 `updated_at >= now`，缓存实际永远查不到。修复：只传 key，使用默认 30 天 TTL。
  2. `set_translation` 用本地时间 `datetime.now()` 写 `updated_at`，而查询用 SQLite `strftime('%s','now')`（UTC），缓存条目恒比 now 大 8 小时，30 天 TTL 形同虚设。修复：改用 `datetime.utcnow()`，与 UTC 对齐。
  3. `translate_readme`（README 翻译）完全不走缓存，每次打开详情都重新调 API。修复：增加与描述翻译一致的缓存读写。
- **切换视图时中断旧任务、释放线程池资源**：`_on_view_changed` 递增翻译代数，进行中的 README 加载、详情/列表翻译、图片嵌入任务检测到代际变化后主动放弃（返回 None），快速释放线程池给新视图任务；列表翻译任务在 `_batch_cancel` 时直接退出。验证：30 条翻译队列切换视图后仅 2 条已启动执行，其余全部取消。
- 版本号 2.3.3 → 2.4.0，重新打包 exe。

## v2.3.3（2026-09-07）
- **修复列表翻译显示进度完成但不显示中文**：`_translate_pump` 的 task 中 `translated, ok = self.service.translate_description(...)` 按元组解包，但 `translate_description` 实际返回字符串，每次翻译都抛 `ValueError` 走 on_error——只累计进度计数，列表项从未更新。修复：改为直接取字符串并判断 `ok = bool(translated) and translated != original`。
- 版本号 2.3.2 → 2.3.3，重新打包 exe。

## v2.3.2（2026-09-07）
- **修复切换趋势热榜不刷新**：`_on_view_changed` 切换到 trending 视图时仅切换页面，不加载数据；`refresh_trending` 无任何调用者（`trending_btn` 从未创建），趋势加载入口完全未接入。修复：切换视图时调用 `refresh_trending()` 拉取最新趋势（on_done 中 `setCurrentIndex(1)` 索引不变时不触发本方法，无递归风险）。
- 版本号 2.3.1 → 2.3.2，重新打包 exe。

## v2.3.1（2026-09-07）
- **修复点击项目即崩溃（0xC0000409）**：复现确认根因——`MainWindow.__init__` 未初始化 `_translation_gen` 属性，点击项目进入 `_show_detail` 时 `self._translation_gen += 1` 抛出 `AttributeError`；PyQt6 对槽内未捕获异常默认触发 qFatal fail-fast 崩溃，用户感知为"一点项目就卡死"。修复：`__init__` 中补初始化 `self._translation_gen = 0`。
- 版本号 2.3.0 → 2.3.1，重新打包 exe。

## v2.3.0（2026-09-07）
- **翻译缓存统计改为 SQL 聚合**：`get_translation_cache_stats` 原实现取出全部缓存值再逐条统计 UTF-8 字节数，缓存大时占用内存与耗时。改为单条 `COUNT(*) + SUM(LENGTH(CAST(value AS BLOB)))` 聚合查询，与缓存裁剪逻辑同款写法，空表由 `COALESCE` 兜底。
- **修复英文目标语言翻译误跳过**：`translate_text` 与 `translate_readme_markdown` 原判断 `target` 以 `en` 开头即返回原文，导致 zh→en 等非英文源翻译被跳过。改为 `source` 与 `target` 均为英文时才跳过，仅过滤无意义的英文→英文翻译。
- **清理冗余赋值语句**：删除 `settings_dialog.py` 中 `self.clear_cache_btn`、`self.translation_test_button` 两处无实际作用的孤立语句。
- 版本号 2.2.0 → 2.3.0，重新打包 exe。

## v2.2.0（2026-09-07）
- **修复详情页闪退（0xC0000409）**：复现确认根因——关闭程序时线程池中运行中的 Worker（QRunnable）被 Python GC 回收，C++ 对象在线程仍在运行时被删除，触发 Qt fail-fast 崩溃。修复：`closeEvent` 中先取消队列翻译、丢弃未启动任务，再 `waitForDone()` 等待运行中线程结束，最后清空 `_active_workers` 引用后才销毁窗口。
- **README 图片嵌入下载限时**：图片串行下载增加 12 秒整体时限，避免图片过多导致关闭程序时长时间等待。
- **项目列表按星级排序**：发现页（搜索）结果按星级从高到低展示。
- **翻译顺序确认**：列表翻译队列按项目顺序 FIFO 入队/出队，从第一个项目开始翻译（无需改动，并发完成顺序不影响启动顺序）。
- 版本号 2.1.0 → 2.2.0，重新打包 exe。

## v2.1.0（2026-09-06）
- **i18n 国际化完善**：详情页操作按钮、下载列表项、批量收藏状态栏、托盘菜单/下载状态、设置缓存统计等残留硬编码中文全部改为 `tr()` 国际化调用，切换英文后界面无中文残留。
- **翻译防卡死改造（五阶段）**：
  1. 翻译专用线程池，与搜索/详情/图片下载隔离；
  2. API 请求总时长硬上限（描述 15s / README 60s）；
  3. 详情翻译拆分为两个独立 Worker（描述先到先显示）；
  4. 列表翻译并发化，`concurrent_limit` 与 `request_interval_ms` 配置生效；
  5. HTTP 连接复用（共用 Session）+ 批量翻译状态栏进度显示。
- 版本号 2.0.0 → 2.1.0，重新打包 exe。
